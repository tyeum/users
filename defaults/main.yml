---
# defaults file for users
users_lock_root: True

users:
    - name: root
      password: '*************'
      #shell: /bin/bash
      #uid: 0
      #groups: sudoers
      #pubkeys:
      #- '{{ lookup("file", "id_rsa.pub") }}'
      #email: none@nowhere.com
