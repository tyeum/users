---
# vars file for ansible-role-users

ssh_daemon:
    OpenBSD: sshd
    Debian: ssh

aliases_file:
    OpenBSD: /etc/mail/aliases
    Debian: /etc/aliases

users_unique_groups: '{{ users|selectattr("groups", "defined")|sum(attribute="groups", start=[])|list|unique }}'
